test_kind: js_test

selector:
  roots:
  - jstests/replsets/*.js
  exclude_files:

executor:
  config:
    shell_options:
      nodb: ''
      readMode: commands
  fixture:
    class: ReplicaSetFixture
    mongod_options:
      set_parameters:
        enableTestCommands: 1
        logComponentVerbosity:
          replication: 4
